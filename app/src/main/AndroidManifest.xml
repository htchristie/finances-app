<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.fmu.financesapp">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinancesApp"
        tools:targetApi="31">
        <activity
            android:name=".ui.activity.GoalsActivity"
            android:exported="false"
            android:label="goals_activity"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".EditGoal"
            android:exported="false"
            android:label="@string/edit_goal"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".EditTransaction"
            android:exported="false"
            android:label="@string/edit_transaction"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".AddGoal"
            android:exported="false"
            android:label="@string/add_goal"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".AddTransaction"
            android:exported="false"
            android:label="@string/add_transaction"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>